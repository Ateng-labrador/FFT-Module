[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "fftModule"
version = "0.1.0"
dependencies = ["numpy"]

[tool.setuptools.packages.find]
where = ["."]
include = ["fftModule*"]
